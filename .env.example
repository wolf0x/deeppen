# DeepPen Configuration
# Copy this file to .env and fill in your values

# Server port (default: 4000)
PORT=4000

# SQLite database path (default: ./data/deeppen.db)
DATABASE_URL=./data/deeppen.db

# Docker container name for CTF tools (default: deeppen-tools)
CONTAINER_NAME=deeppen-tools
